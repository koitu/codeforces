#include <bits/stdc++.h>
using namespace std;


void solve() {
    int r, c;
    cin >> r >> c;

    vector<vector<char>> m(r, vector<char>(c));

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    solve();
}
