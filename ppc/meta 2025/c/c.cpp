#include <bits/stdc++.h>
using namespace std;


void solve() {
    int n;
    cin >> n;

    // sum the len of all contiguous subarrays in A
    // for each subarray of A where XORing leads to zero subtract the size and add the numbers (without the last one)

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    for (int i = 0; i < t; i++) {
        cout << "Case #" << i+1 << ": ";
        solve();
    }
}
