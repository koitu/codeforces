#include <bits/stdc++.h>
using namespace std;


int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n, m;
    int u, v, w;
    for (int i = 0; i < m; i++) {
        cin >> u >> v >> w;
    }
    cout << 0 << endl;
}
